# Known limitations

The full original Python environment is not evidenced in the locked run manifest; a clean-environment reproduction check is required before release. Temporal validation is not external validation, and the recorded outcome depends on testing intensity.
